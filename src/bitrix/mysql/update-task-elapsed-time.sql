UPDATE b_tasks_elapsed_time
SET
    DATE_START=DATE_ADD(`DATE_START`, INTERVAL -86400 SECOND),
    DATE_STOP=DATE_ADD(`DATE_START`, INTERVAL -86400 + `SECONDS` SECOND)
WHERE
    ID IN (8158, 8159, 8160, 8161)
